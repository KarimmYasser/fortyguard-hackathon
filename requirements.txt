fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.8.0
pydantic-settings>=2.4.0
httpx>=0.27.0
requests>=2.31.0
langgraph>=0.2.0
langchain-core>=0.3.0
openai>=1.40.0
numpy>=1.26.0
pandas>=2.0.0
# scikit-learn is deliberately NOT installed. It pulls scipy (112 MB) and
# sklearn (48 MB), taking the bundle to ~435 MB against Vercel's 250 MB
# unzipped lambda cap. src/data_science/ml_models.py therefore runs a numpy
# z-score fallback for anomaly detection and a closed-form physics
# approximation for the surrogate, and reports backend="unavailable"
# rather than inventing trained-model accuracy metrics.
python-dotenv>=1.0.0
