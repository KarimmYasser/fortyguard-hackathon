{
  "name": "thermal-sentinel-grid",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend run build",
    "record:business-demo": "node scripts/record_business_value_demo.mjs"
  },
  "devDependencies": {
    "puppeteer": "^25.8.0"
  }
}
