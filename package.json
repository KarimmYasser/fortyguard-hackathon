{
  "name": "thermal-sentinel-grid",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "puppeteer": "^25.8.0"
  }
}
